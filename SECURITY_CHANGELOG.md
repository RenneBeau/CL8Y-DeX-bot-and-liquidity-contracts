# Security Change Log

This is an internal traceability record, not an audit or proof of security. Commit
timestamps show publication order only; short intervals do not prove whether a
review occurred. No pull-request review evidence or signed commit was identified
for the entries below.

> **Historical chronology:** rows describe behavior at the listed revision and
> may be superseded. In particular, the 2026-08-06 per-LP designs are not current.
> Current code tiers only limit-grid `bot.owner`, market-grid `config.admin`, and
> rebalancer `config.admin`; it converts economic fee value to LP shares through
> current NAV. Current production deployment is blocked by unavailable approved
> registry/collector/proxy addresses and topology evidence, deployment migration,
> external DEX semantics, unrun exact-candidate release evidence, and independent
> review. See `docs/FEE_TIER_PROTOCOL.md`, `docs/DEPLOY_FEE_SYSTEM.md`, and
> `RELEASE.md`.

| Commit | Time (UTC+02) | Author | Change and surface | Tests/evidence at commit | Residual risk |
|---|---|---|---|---|---|
| `a8fd57a` | 2026-08-01 11:12:46 | rennebeau | Grid reconciliation/operator and bot-vault rebalance hardening; 16 files, contracts, operator, deployment and docs | Operator and vault tests changed; later baseline CI exists, no independent review identified | Cross-system change was broad; ambiguous grid terminal classification remained |
| `e829c3e` | 2026-08-01 12:09:03 | rennebeau | Persist ambiguous rebalancer broadcast state; keeper implementation/docs/tests | 51 keeper-test lines added; later source CI exists | Operator intervention remains required when no hash was durably recorded |
| `9daa295` | 2026-08-01 12:15:46 | rennebeau | Confirmation depth in both keepers; 8 files | Keeper and operator tests added; later source CI exists | Chain-specific finality assumptions remain operational |
| `cc8b486` | 2026-08-01 12:20:48 | rennebeau | Bot-liquidity bootstrap minimum; contract/errors/docs | Contract tests added later; later source CI exists | Economic minimum remains deployment-specific |
| `8806ee6` | 2026-08-01 12:43:55 | rennebeau | Grid parked-escrow solvency reporting; contract/message/docs | Unit coverage added; later source CI exists | Diagnostic inherited ambiguous active-query classification, corrected in current worktree |
| Uncommitted issue #46 work | 2026-08-01 | OpenCode/user worktree | Bounded pair-first legacy vault inventory drain, empty rescan proof, stale-local cleanup and exact balance synchronization | Local invariant tests and release checks must pass before commit | Depends on unmerged upstream PR #1; superseded by the cancel-ledger redesign below |
| Uncommitted limit-grid autonomy work | 2026-08-05 | OpenCode/user worktree | Drop the pair inventory-reconciliation dependency: the vault records its own cancels (`CANCELLED_ORDERS`) and classifies any order that vanished without a recorded cancel as fully executed; solvency split into `executed_orders`/`cancelled_orders`; migration no longer scans the pair | `cargo test --workspace` green (4 manager + 29 vault unit + 12 integration); `cargo clippy --all-targets -- -D warnings` clean | The "unknown means fully executed" default trusts that the shipped pair only ever loses an order to a fill or a vault-initiated cancel; an order missing with a healthy parked query is classified, not rejected; no independent review |
| Uncommitted fee-system + protocol fee | 2026-08-05 | OpenCode/user worktree | New `fee-system` workspace (fee-registry + fee-collector) and grid-vault protocol fee per fill: canonical tier ladder seeded at instantiate (`ladder_version`), `RefreshHolding` persistence (user-directed: persist via message so it is always readable back), collector-only `RedeemShares`, value-as-token-0 fee minted as LP to the collector (dilutes holders) | `cargo test --workspace` green (limit-grid 48 = 4 manager + 29 unit + 15 integration incl. fee tests; fee-system 10); `cargo clippy --all-targets -- -D warnings` clean | Fees depends on a live fee-registry read and the reference-price conversion assumption mirrored from `deposit_shares`; no independent review |
| Uncommitted audit fixes (live-first + non-blocking fee) | 2026-08-05 | OpenCode/user worktree | (1) Registry `EffectiveFee` became live-first (current CL8Y balance), with persisted `RefreshHolding` data on a transient live-read failure and the lowest tier (full base fee) when no cache exists; a stale cache can overcharge or undercharge relative to the unknown current balance; (2) vault `charge_fee` became non-blocking — a registry query failure skips the fee (`fee_skipped`) instead of reverting reconcile; (3) defensive `fee_bps` cap at 10 000 | `cargo test --workspace` green (limit-grid 48, fee-system 10); `cargo clippy --all-targets -- -D warnings` clean on both | Vault-level registry failure can skip all fees; cached fallback is availability-oriented, not a conservative lower bound; no independent review |
| Uncommitted market-grid + rebalancer per-execution fee | 2026-08-05 | OpenCode/user worktree | Carry the limit-grid per-fill protocol fee to the two remaining vaults so all three systems tax the executed swap value (never holdings). `grid-vault-swap` (market-grid): `fee_registry`/`fee_collector` in instantiate + `UpdateConfig`, fee minted as LP to the collector in the settle reply (dilutes holders), collector-only `RedeemShares`. `bot-vault` (rebalancer): same fee config, a token-0-normalized `FEE_SHARES` value ledger accrued per executed rebalance swap in the settle reply, collector-only `RedeemShares` paying a pro-rata slice of both balances to the treasury recipient, admin-gated `UpdateFeeConfig`. Both reuse the live-first/non-blocking `charge_fee` (fail → `fee_skipped`) | `cargo test --workspace` green (market-grid 19 = 10 unit + 9 integration incl. fee+redeem path; rebalancer bot-vault 18 incl. admin gating, collector-only redeem, no-fee-without-config); `cargo clippy --all-targets -- -D warnings` clean; on-chain E2E `fee-e2e-multi.sh` on LocalTerra (market-grid 180 bps/live/fee_shares 34609105, rebalancer 180 bps/live/fee_shares 81626937, both treasury-paid and collector shares net to 0) | Fee valuation still uses the reference-price (TWAP) token-0 normalization mirrored from `deposit_shares`; rebalancer `FEE_SHARES` redeems against current vault balances (economic exposure to subsequent price moves until collected); no independent review |
| Uncommitted full fee-audit pass | 2026-08-05 | OpenCode/user worktree | Fresh-eyes audit of the entire protocol-fee path with an expanded test matrix: fee-registry ladder tested tier-by-tier at exact CL8Y boundaries and one wei below, a 31-point balance matrix cross-checked against an independent reference resolver, base-fee edges (0..10000 bps), a governance-added 100% discount tier reaching fee zero, reserved-tier add/update rejection; limit-grid 100%/0%/1 bps rate boundaries; market-grid non-blocking proof with a valid-but-empty registry address; bot-vault `charge_fee` exact-math and non-blocking/zero-rate unit tests | `cargo test --workspace --all-targets` green (fee-system 22 = 10 registry + 8 audit tiers + 4 collector; limit-grid 50; market-grid 20; rebalancer 35); `cargo clippy --workspace --all-targets -- -D warnings` clean on all four workspaces; `docs/TEST_RESULTS.md` audit section updated | No mechanism yet assigns addresses to governance-only tiers 0/255; integer flooring reduces collected fee and favors the payer; no independent review |
| `45f1eba` | 2026-08-05 | rennebeau | Committed full fee-audit pass (see row above; fee-system 22 / limit-grid 50 / market-grid 20 / rebalancer 35 tests, clippy `-D warnings` clean on all four workspaces) | Tests/evidence above | See prior row |
| `b2fc0e7` | 2026-08-06 | rennebeau | Committed per-LP fee-tier rework + prod addresses: `grid-vault-swap` (market-grid) `charge_fee` reworked from a single vault-address lookup to **per-LP**: each holder is taxed at their own CL8Y tier (holder slice = `value × shares / total`), burned from their own shares, credited to the collector; total conserved (no dilution); event carries `fee_holders` + weighted-average `fee_bps`. NOTE (superseded by the row below): `bot-vault` was initially re-pointed to `config.admin`, which is wrong for the pooled rebalancer and was corrected. Added `each_lp_is_taxed_at_their_own_tier` (different-tiers holder diff + conservation). Added `docs/DEPLOY_FEE_SYSTEM.md` (production mainnet CL8Y + CMM-treasury addresses, both validated 32-byte bech32, with fee-registry / fee-collector instantiation flow) and wired `CL8Y_FEE_REGISTRY`/`CL8Y_FEE_TREASURY`/`CL8Y_FEE_COLLECTOR` env overrides into `test-area/deploy-system.sh` (dummy fallback) | `cargo test --workspace --all-targets` green (market-grid 21 = 10 unit + 11 integration; rebalancer 35); `cargo clippy --workspace --all-targets -- -D warnings` clean on both workspaces | The rebalancer was still single-owner (`config.admin`) — incorrect for a pool; corrected in the next row. Per-holder multi-query increases registry load per fill; weighted `fee_bps` event can round below a single holder's exact rate; no independent review |
| Uncommitted rebalancer per-LP (poly) correction | 2026-08-06 | OpenCode/user worktree | The rebalancer MUTUALISES several traders into one pooled bot-vault, so charging `config.admin` (mono) was wrong. `bot-vault` `charge_fee` is now **poly**: it enumerates the pooled `bot-liquidity` LP token (`TokenInfo` total supply + paginated `AllAccounts` + per-holder `Balance`), attributes each holder's slice of the fill at THEIR OWN CL8Y tier, and accrues the sum to the collector's `FEE_SHARES` value claim. `FeeApplied`/reply gain `fee_holders`; single-holder reports the exact tier with `source = "live"`, mixed tiers report `source = "pooled_tiers"` and no single tier. Unit tests rewritten around a pooled mock (two holders at different rates with weighted fee + conservation, single holder, non-blocking registry failure, zero-rate skip) | `cargo test --workspace --all-targets` green (rebalancer 36 = 11 bot-liquidity + 21 bot-vault + 4); `cargo clippy --workspace --all-targets -- -D warnings` clean; `docs/TEST_RESULTS.md` updated | Per-holder enumeration adds N `AllAccounts`/`Balance`/`EffectiveFee` queries per fill (gas cost grows with pool size; unbounded pools are not yet bounded/capped); a single registry failure still skips the whole charge non-blockingly; the collector's value claim redeems against current vault balances (price exposure until collected); no independent review |
| Uncommitted audit-fix working tree | 2026-08-08 | OpenCode/user worktree | NAV-priced fee shares in all venues; solvent limit Exit; market boundary/offer/migration/deposit fixes; manager fee propagation; multi-vault-per-pair proxy; compile-time canonical-address environment gates; four-workspace CI/release/manifests; operator state/transaction hardening; strict fee E2E target/workflow | `make test`, `make clippy`, shell syntax, common helper tests, and workflow YAML parsing reported PASS. Current totals: rebalancer keeper 58, grid operator 65. Canonical LocalTerra fee E2E was not run in this working tree; complete reproducible builds are not claimed | At this stage production addresses, pair provenance/external semantics, remaining findings, independent audit, and current-SHA canonical fee E2E remained open; pair provenance and expanded pinning were superseded by the next row |
| Latest uncommitted audit remediation | 2026-08-08 | OpenCode/user worktree | Mainnet pinning now covers fee registry, collector, and proxy as applicable; all vaults cache the last successful effective fee by fee subject and charge it during registry outages; registry token-query failure grants no discount; market/rebalancer require factory registration plus pinned runtime pair code and recheck before execution; affected contracts/schemas are `0.2.0`; collector cumulative accounting uses checked addition; paused market vaults allow safe pro-rata exits | `make test`, `make clippy`, `bash -n`, `test-area/common-test.sh`, workflow YAML parse, and missing-registry compile-failure check reported PASS. Python: rebalancer keeper 58, grid operator 65. Rust contract counts increased; no stale aggregate is asserted | Approved real registry/collector/proxy addresses do not exist; canonical LocalTerra fee E2E and `0.2.0` redeployment were not run; external DEX semantics, remaining findings, and independent review remain open |
| Final uncommitted documentation reconciliation | 2026-08-08 | OpenCode/user worktree | Late fills close exactly once through verified reconciliation execution height; typed audited intervention and checksummed JSON/SQLite recovery; production-compatible credential/external-signer models; frozen raw-state fixtures for all seven migrations with contract-specific 0.2 redeploy policy; 128-case proptest invariants; isolated Python package/lint/type/coverage gates | `make test`, `make clippy`, `bash -n`, `test-area/common-test.sh`, and workflow YAML parsing reported PASS. Python: rebalancer keeper 63 tests/72.84% combined branch coverage; grid operator 71 tests/76.08%; 70% branch baseline. Missing canonical registry compile failure verified | Internal audit ledger: 33 closed/doc-corrected, 3 partial (H-05, H-08, M-30), 2 open (M-15, M-32), total 38. Production BLOCKED. Full canonical LocalTerra fee E2E and full reproducible release artifact set were not run; no independent audit is claimed |
| Limit-grid production de-scoping | 2026-08-08 | User decision | Limit-grid is abandoned as a production venue but remains built, tested, and published as a PoC artifact. M-15 and M-32 are formally accepted out of production scope; their underlying DEX terminal-state and direct-maker fee semantics were not technically validated | Documentation-only scope decision; prior source verification remains unchanged | Internal audit ledger: 35 closed/doc-corrected/de-scoped, 3 partial (H-05, H-08, M-30), 0 open, total 38. Production remains BLOCKED for the three partial findings and independent release evidence |
| H-08 and M-30 closure | 2026-08-08 | OpenCode/user worktree | Rebalancer `real_registry_ladder` now runs actual-contract `cw-multi-test` full settlement for no-holder and tiers 1..9 through collector withdrawal. Authoritative release policy now tiers production versus PoC, enforces exact stable tags and production version matching, derives inventory counts, and scopes the expiring RustSec exception to the exact package/version in all four lockfiles | `make test`, `make clippy`, release-policy fixtures, release inventory, live RustSec policy, shell syntax, and workflow YAML parse PASS. `cargo deny` reported PASS by implementation agent. Full release/reproducible Docker artifacts, candidate tag workflow, and canonical LocalTerra fee E2E were not run | Internal audit ledger: 37 closed/doc-corrected/de-scoped, 1 partial (H-05), 0 open, total 38. Production remains BLOCKED solely at finding level by H-05; independent audit and exact-candidate release evidence remain release gates |

The commits were close in time and interacted with shared custody, keeper, and
documentation surfaces. Observable evidence supports active remediation and
later automated source checks, but not independent review, signed authorship, or
a stabilization period. References to "current uncommitted" work are historical
wording from those rows, not a description of the present worktree. Any current
status claim requires exact-SHA CI, migration rehearsal, and review evidence.

For the limit grid, the pair inventory-reconciliation flow (issue #46, upstream
PR #1) has been removed in favor of the vault-local cancel ledger. The vault no
longer depends on pair-side owner-inventory queries, snapshot generations, or a
withdrawal lock; it reconciles directly against current pair state. Operators must
still monitor `OrderStatusUnverifiable` rows (both active and parked queries
failed) and retry after pair/RPC health is restored.
